<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.utfpr.neto.projetofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".JogosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".JogoActivity"
            android:parentActivityName=".JogosActivity" />
        <activity
            android:name=".EstadosActivity"
            android:parentActivityName=".JogosActivity" />
        <activity
            android:name=".EstadoActivity"
            android:parentActivityName=".EstadosActivity" />
        <activity
            android:name=".PlataformasActivity"
            android:parentActivityName=".JogosActivity" />
        <activity
            android:name=".PlataformaActivity"
            android:parentActivityName=".PlataformasActivity" />
        <activity
            android:name=".SobreActivity"
            android:parentActivityName=".JogosActivity" />
        <activity
            android:name=".CorActivity"
            android:parentActivityName=".JogosActivity" ></activity>
    </application>

</manifest>